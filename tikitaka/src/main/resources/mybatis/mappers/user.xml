<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<select id="findByIdAndPassword" parameterType="map" resultType="user">
		<![CDATA[
		select *
		  from user
		 where email=#{email }
		   and password=#{pwd }
		]]>
	</select>
	<insert id="insertUser" parameterType="user">
		<![CDATA[
			insert into user values(null, #{role}, #{name}, #{password}, #{phone}, #{email}, null, now(), now(), 0, null, null)
		]]>
	</insert>
	
	<select id="findLogStatus" parameterType="int" resultType="user">
	 	<![CDATA[	
	 		SELECT status 
	 		FROM user 
	 		WHERE no=#{no}
	 	]]>
	</select>
</mapper>
